# Frontend Environment Variables (Optional)

# API Base URL - Uses proxy to backend by default
# Only change if backend runs on different host/port
VITE_API_URL=/api

# Example for remote backend:
# VITE_API_URL=https://your-backend-domain.com
